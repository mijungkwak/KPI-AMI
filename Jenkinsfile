pipeline {
  agent any
  stages {
    stage('AMI Create') {
      steps {
        sh 'echo "AMI Create"'
      }
    }

    stage('Instance Create') {
      steps {
        sh 'echo "Instance Create"'
      }
    }

    stage('Code Deploy') {
      steps {
        sh 'echo "Code Deploy"'
      }
    }

  }
}